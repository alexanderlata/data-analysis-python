# Предобработка данных: выявление выбросов в данных

## Задание 1: Анализ сна (датасет sleep75)

### Контекст

Исследователи изучают режим сна работающих американцев. В датасете `sleep75.csv` есть переменная `sleep` — количество минут сна в неделю. Некоторые значения могут быть ошибочными или представлять экстремальные случаи.


### Задачи

**1.1. Проведите первичный анализ переменной `sleep`:**
- Выведите описательную статистику (`df['sleep'].describe()`)
- Постройте гистограмму и коробчатую диаграмму (boxplot)
- Сколько минут в среднем спят люди в неделю? Переведите в часы в день

**1.2. Выявите выбросы методом IQR:**
- Вычислите Q1, Q3, IQR
- Определите границы выбросов
- Сколько наблюдений являются выбросами?
- Какие это значения? Реалистичны ли они?

**1.3. Примените метод Z-score:**
- Вычислите Z-оценки для всех значений
- Найдите наблюдения с |Z| > 3
- Сравните результаты с методом IQR

**1.4. Анализ выбросов:**
- Есть ли люди, которые спят менее 5 часов в сутки?
- Есть ли люди, которые спят более 12 часов в сутки?
- Можно ли считать эти значения ошибками измерения?

**1.5. Создайте переменную `sleep_hours_per_day = sleep / (60 * 7)` и повторите анализ выбросов. Изменились ли результаты?**


---

## Задание 2: Почасовая оплата труда (датасет wage1)

### Контекст

Экономисты анализируют факторы, влияющие на заработную плату. Нужно проверить данные `wage1.csv` на наличие аномальных значений зарплаты.

### Задачи

**2.1. Исследуйте переменную `wage`:**

Постройте коробчатую диаграмму (boxplot) и гистограмму, затем ответьте на вопросы:

**Вопросы:**
1. Какой тип распределения у переменной wage? (симметричное, правостороннее скошенное, левостороннее скошенное)
2. Чему равна разность между средним и медианой?
3. Что это говорит о форме распределения?
4. Какой метод выявления выбросов лучше использовать и почему?
5. Почему зарплаты часто имеют правостороннюю асимметрию?

**2.2. Выявите выбросы в зарплате методом IQR:**
- Найдите границы выбросов
- Выведите список наблюдений-выбросов
- Какова максимальная и минимальная зарплата среди выбросов?

**2.3. Примените метод Z-score:**
- Подходит ли этот метод для скошенных данных?
- Сравните количество выбросов с методом IQR
- Какой метод более консервативен для этих данных?

**2.4. Создайте функцию `analyze_wage_outliers(df)`, которая:**
- Принимает DataFrame
- Возвращает DataFrame с выбросами
- Включает колонки: wage, educ, exper, метод обнаружения

**2.5. Исследуйте характеристики людей с аномальной зарплатой:**
- Какое у них образование?
- Сколько лет опыта?
- Есть ли закономерности?

---

## Задание 3: Цены на бриллианты (датасет Diamond)

### Контекст

Ювелирная компания анализирует ценообразование бриллиантов. Необходимо выявить аномальные цены в данных `Diamond.csv`, которые могут быть ошибками ввода данных.

### Задачи

**3.1. Предварительный анализ:**
- Постройте scatter plot: carat vs price
- Есть ли видимая зависимость?
- Есть ли визуально заметные выбросы?

**3.2. Анализ цены (price):**
- Выявите выбросы методом IQR
- Постройте boxplot с выделением выбросов
- Какие бриллианты имеют аномально высокую/низкую цену?

**3.3. Создайте новую переменную — цена за карат:**
```python
df['price_per_carat'] = df['price'] / df['carat']
```
- Найдите выбросы по price_per_carat
- Постройте boxplot для price_per_carat
- Какие бриллианты имеют аномальную цену за карат?

**3.4. Многомерный анализ:**
- Постройте boxplot цены для каждого цвета (colour)
- Есть ли выбросы внутри каждой группы?
- Используйте `df.groupby('colour')` для группового анализа

**3.5. Комплексная проверка:**
- Создайте функцию, которая находит бриллианты, являющиеся выбросами одновременно по:
  * Цене
  * Весу (carat)
  * Цене за карат
- Сколько таких "тройных выбросов"?
- Что в них необычного?

**3.6. Напишите отчет:**
- Какие записи рекомендуется проверить?
- Какие могут быть причины аномалий? (ошибки ввода, уникальные камни, мошенничество)

---

## Задание 4: Успеваемость студентов (Student Performance)

### Контекст

Университет собрал данные о факторах, влияющих на успеваемость. Нужно выявить студентов с аномальными показателями для дополнительной проверки данных `student_performance.csv`.

### Задачи

**4.1. Проверка на физическую возможность:**
- Найдите студентов, которые спят менее 3 или более 12 часов
- Найдите студентов, которые учатся более 16 часов в день
- Являются ли эти значения выбросами статистически?

**4.2. Анализ Performance Index:**
- Визуализируйте распределение индекса успеваемости
- Найдите выбросы обоими методами (IQR и Z-score)
- Симметрично ли распределение?

**4.3. Выявление "суперстудентов" и "проблемных случаев":**
- Найдите студентов с Performance Index > 95 (метод процентилей)
- Найдите студентов с Performance Index < 20
- Проанализируйте их характеристики (сколько учатся, спят, практикуются)

**4.4. Создайте комплексную функцию проверки:**

```python
def detect_anomalous_students(df):
    """
    Находит студентов с аномальными показателями
    Возвращает DataFrame с флагами аномалий
    """
    # Ваш код здесь
    pass
```

Функция должна добавлять колонки:
- `outlier_performance`: выброс по успеваемости
- `outlier_study_hours`: аномальные часы учебы
- `outlier_sleep`: аномальный сон
- `suspicious`: True если 2+ аномалии

**4.5. Парадоксы в данных:**
- Найдите студентов, которые мало учатся (< 2 часа), но имеют высокую успеваемость (> 80)
- Найдите студентов, которые много учатся (> 8 часов), но имеют низкую успеваемость (< 40)
- Сколько таких "парадоксальных" случаев?

**4.6. Визуальный отчет:**
- Создайте 2x2 сетку boxplot для всех числовых переменных
- Отметьте выбросы разными цветами
- Добавьте аннотации для экстремальных значений

---

## Задание 5: Производство электроэнергии (Electricity)

### Контекст

Энергетическая компания анализирует эффективность производства на разных электростанциях по данным `electricity.csv`. Аномальные значения могут указывать на проблемы или уникальные условия работы.

### Задачи

**5.1. Анализ структуры затрат:**
- Проверьте, что `sl + sk + sf ≈ 1` для всех наблюдений
- Найдите наблюдения, где сумма значительно отличается от 1
- Это технические ошибки или особенности учета?

**5.2. Выявление неэффективных производств:**
- Создайте переменную: `cost_per_unit = cost / q`
- Найдите выбросы по стоимости единицы продукции
- Постройте scatter plot: q vs cost (выделите выбросы цветом)

**5.3. Многомерный анализ выбросов:**

Для каждой переменной (cost, q, pl, pk, pf):
- Найдите выбросы методом IQR
- Создайте матрицу (5x5) с отметками выбросов
- Есть ли наблюдения, которые являются выбросами по нескольким параметрам?

---

## Задание 6: Большой датасет бриллиантов (diamonds - 53,940 наблюдений)

### Контекст

Крупнейший онлайн-ретейлер бриллиантов анализирует свой каталог из почти 54 тысяч камней (`diamonds.csv`). Необходимо выявить аномалии в измерениях и ценах.

### Задачи

**6.1. Проверка физической корректности:**
- Найдите бриллианты с x = 0, y = 0 или z = 0
- Найдите бриллианты, где `depth != 2 * z / (x + y)` (с погрешностью 1%)
- Найдите бриллианты с аномальными пропорциями (y > 2*x или x > 2*y)
- Сколько физически некорректных записей?

**6.2. Анализ цены с учетом размера:**
- Постройте log(price) vs log(carat) scatter plot (выборка 1000 точек)
- Создайте модель: `expected_price = a * carat^b` (подберите параметры или используйте регрессию)
- Найдите бриллианты, где `actual_price / expected_price` — выброс

**6.3. Стратифицированный анализ выбросов:**

```python
# Для каждой комбинации cut-color-clarity:
# - Найдите выбросы по цене внутри группы
# - В каких группах больше всего выбросов?
```

---

## Задание 7: Комплексный кейс — Зарплаты (wage2)

### Контекст

Департамент труда проводит масштабное исследование факторов, влияющих на зарплату. В данных 935 работников (`wage2.csv`). Необходим полный аудит данных перед анализом.


### Комплексные задачи

**7.1. Создайте автоматизированный пайплайн анализа выбросов:**

```python
class OutlierDetector:
    def __init__(self, methods=['iqr', 'zscore']):
        self.methods = methods
        self.outliers_dict = {}
    
    def fit(self, df, columns):
        """Находит выбросы во всех указанных колонках"""
        pass
    
    def get_report(self):
        """Возвращает детальный отчет"""
        pass
    
    def visualize(self):
        """Создает визуализацию"""
        pass
```

**7.2. Проверка логической согласованности:**
- Проверьте: `age >= educ + 6` (нельзя закончить школу раньше 6 лет)
- Проверьте: `exper <= age - educ - 6` (опыт не может быть больше возможного)
- Найдите логически несовместимые записи

**7.3. Анализ почасовой ставки:**
- Создайте: `hourly_wage = wage / (hours * 4.33)`
- Найдите выбросы по почасовой ставке
- Есть ли люди с зарплатой менее минимальной? Более $100/час?

**7.4. Исследование взаимосвязей:**
- Постройте матрицу корреляций для wage, IQ, educ, exper
- Найдите наблюдения с высоким IQ, но низкой зарплатой (выбросы от тренда)
- Найдите наблюдения с низким образованием, но высокой зарплатой

**7.5. Сегментированный анализ:**

Проанализируйте выбросы отдельно для:
- Мужчин и женщин
- Разных регионов (south, urban)
- Женатых/холостых

Различаются ли паттерны выбросов между группами?

**7.6. Финальный отчет (в формате Jupyter Notebook):**
- **Executive Summary:** сколько проблемных записей, какие типы аномалий
- **Детальный анализ** по каждой переменной
- **Рекомендации:** что удалить, что проверить, что оставить
- **Оценка влияния** выбросов на потенциальный регрессионный анализ

---

## 💡 Общие рекомендации для всех заданий

### Структура решения

**1. Импорт библиотек**

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from scipy import stats
```

**2. EDA (Exploratory Data Analysis)**

```python
# Первые строки
df.head()

# Информация о данных
df.info()

# Статистика
df.describe()
```

**3. Выявление выбросов**

```python
# Метод IQR
Q1 = df['column'].quantile(0.25)
Q3 = df['column'].quantile(0.75)
IQR = Q3 - Q1
lower_bound = Q1 - 1.5 * IQR
upper_bound = Q3 + 1.5 * IQR

# Метод Z-score
z_scores = np.abs(stats.zscore(df['column']))
outliers_zscore = df[z_scores > 3]
```

**4. Визуализация**

```python
# Boxplot
plt.figure(figsize=(10, 6))
plt.boxplot(df['column'])
plt.title('Boxplot')
plt.show()

# Гистограмма
plt.figure(figsize=(10, 6))
plt.hist(df['column'], bins=30)
plt.title('Histogram')
plt.show()
```

**5. Интерпретация и выводы**
- Что означают найденные выбросы?
- Это ошибки или реальные экстремальные случаи?
- Что делать с выбросами?

### Шаблон функции для анализа выбросов

```python
def detect_outliers(data, method='IQR'):
    """
    Функция для выявления выбросов
    
    Parameters:
    -----------
    data : array-like
        Массив данных
    method : str
        Метод ('IQR' или 'zscore')
    
    Returns:
    --------
    dict : словарь с результатами анализа
    """
    data = np.array(data)
    
    if method == 'IQR':
        Q1 = np.percentile(data, 25)
        Q3 = np.percentile(data, 75)
        IQR = Q3 - Q1
        lower = Q1 - 1.5 * IQR
        upper = Q3 + 1.5 * IQR
        
        outliers_mask = (data < lower) | (data > upper)
        
    elif method == 'zscore':
        z_scores = np.abs(stats.zscore(data))
        outliers_mask = z_scores > 3
        lower, upper = data.mean() - 3*data.std(), data.mean() + 3*data.std()
    
    return {
        'outliers': data[outliers_mask],
        'outliers_indices': np.where(outliers_mask)[0],
        'n_outliers': np.sum(outliers_mask),
        'lower_bound': lower,
        'upper_bound': upper,
        'clean_data': data[~outliers_mask]
    }
```

---


## 📚 Полезные ресурсы

### Документация

- [Pandas Documentation](https://pandas.pydata.org/docs/)
- [NumPy Documentation](https://numpy.org/doc/)
- [Matplotlib Documentation](https://matplotlib.org/stable/contents.html)
- [Seaborn Documentation](https://seaborn.pydata.org/)
- [SciPy Stats](https://docs.scipy.org/doc/scipy/reference/stats.html)

### Дополнительное чтение

- [Understanding Boxplots](https://towardsdatascience.com/understanding-boxplots-5e2df7bcbd51)
- [Detecting Outliers in Python](https://towardsdatascience.com/ways-to-detect-and-remove-the-outliers-404d16608dba)
- [Z-Score vs IQR Method](https://statisticsbyjim.com/basics/outliers/)

---

## 🎯 Контрольные вопросы

1. В чем разница между методом IQR и Z-score?
2. Когда следует использовать каждый из методов?
3. Что такое робастность метода?
4. Почему среднее чувствительно к выбросам, а медиана — нет?
5. Всегда ли нужно удалять выбросы?
6. Как форма распределения влияет на выбор метода?
7. Что показывает коэффициент skewness?
8. Как интерпретировать boxplot?
9. Что такое "усы" в boxplot и как они вычисляются?
10. Могут ли выбросы быть полезными для анализа?

---

## ✅ Чек-лист сдачи работы

Убедитесь, что:

- [ ] Все задачи выполнены
- [ ] Код запускается без ошибок
- [ ] Присутствуют все необходимые графики
- [ ] Графики имеют подписи осей и заголовки
- [ ] Код содержит комментарии
- [ ] Написаны выводы и интерпретации
- [ ] Ответы на все вопросы в заданиях
- [ ] Файлы правильно названы
- [ ] Проверена орфография в комментариях

